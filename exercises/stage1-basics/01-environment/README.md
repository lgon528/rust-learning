# 练习 1：环境设置

## 目标

验证你的 Rust 开发环境已正确设置，并能成功编译和运行一个简单的 Rust 程序。

## 任务

1.  导航到 `exercises/stage1-basics/01-environment/exercise` 目录。
2.  使用 `cargo run` 命令编译并运行该项目。
3.  确认程序成功输出 "Hello, world!"。

## 验收标准

-   `cargo run` 命令执行成功，没有任何错误。
-   控制台输出 `Hello, world!`。