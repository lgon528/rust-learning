# 区块链练习

## 目标

扩展 `src/main.rs` 文件中的简单区块链实现，以包括一个工作量证明（Proof of Work）系统。

## 提示

- 向 `Block` 结构添加一个 `nonce`（随机数）字段。
- 创建一个 `mine_block` 函数，该函数会不断递增 `nonce`，直到找到一个哈希值，该哈希值以一定数量的零开头（例如，“0000”）。
- 修改 `add_block` 函数以在添加新区块之前对其进行挖掘。