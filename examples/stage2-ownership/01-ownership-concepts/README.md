# 01-ownership-concepts

本示例演示了 Rust 的所有权系统，包括：

*   **所有权（Ownership）**：变量如何拥有值。
*   **移动（Move）**：值如何在变量之间转移所有权。
*   **克隆（Clone）**：如何显式复制数据。
*   **借用（Borrowing）**：如何通过引用访问数据而不获取所有权。
*   **可变借用（Mutable Borrows）**：如何修改被借用的数据。
*   **切片（Slices）**：如何引用集合的一部分。

## 如何运行

```bash
cargo run
```